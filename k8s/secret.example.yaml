# Template only - copy to secret.yaml (gitignored) and fill in real values.
# See README "Running multiple instances" for how to generate the JWT keys
# and apply this.
apiVersion: v1
kind: Secret
metadata:
  name: lost-and-found-secrets
type: Opaque
stringData:
  DB_USERNAME: lostandfound
  DB_PASSWORD: lostandfound
  APP_JWT_PRIVATE_KEY: "<base64 PKCS8 DER private key - see README>"
  APP_JWT_PUBLIC_KEY: "<base64 X509 DER public key - see README>"
